# Support keys used by references/js-dom-and-lists.md

Rows for support.md §A. Checked 2026-09-23 against the research copies of MDN browser-compat-data 8.1.2 (build 2026-09-17) and web-features (`raw/bcd.json`, `raw/web-features.json`), and against `verify/04` (popover, dialog, inert, `hidden="until-found"`, `<details name>`: verified). The anchor positioning, `moveBefore()` and Custom Highlight facts come from `15-gaps-round-2.md` (no verify report yet); I re-read each version in the raw BCD and web-features copies, and they match.
`dialog`, `inert` and `<template>` are Baseline Widely available, so DOM-07 and DOM-09 name them with `baseline` and need no row.

| Key | What it covers | Chrome / Edge | Baseline | Fallback in code | Rules | Checked | Source |
|---|---|---|---|---|---|---|---|
| `popover` | `popover` attribute, `popovertarget`, `showPopover()` and `hidePopover()`: top layer, light dismiss, focus return | 114 (BCD `html.global_attributes.popover`); web-features gives 116 at feature level | newly available, low 2025-01-27 | none | DOM-09 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes/popover |
| `invoker-commands` | `commandfor` and `command` on `<button>` (`toggle-popover`, `show-modal`, `close`, and others) | 135 (BCD `html.elements.button.commandfor`, `api.CommandEvent`) | newly available, low 2025-12-12 | below the floor: `popovertarget` for popovers, a click listener that calls `showModal()` | DOM-09 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/API/Invoker_Commands_API |
| `anchor-positioning` | `anchor-name`, `anchor()`, `anchor-size()`, `@position-try`, `position-try-fallbacks`, `position-area`, `position-anchor` | core 125; `position-try-fallbacks` 128; `position-area` 129 (several single keywords, for example `block-end` and `top`, full only from 144); `position-anchor` full from 151, because its initial value was `implicit` in 125–126, `auto` in 127–143 and `none` in 144–150 (BCD notes); anchors moved by `transform` from 144 (web-features `anchor-positioning-transforms`) | group `anchor-positioning`: limited (not Baseline); per key newly available: core low 2026-01-13, `position-try-order` and several `position-area` keywords 2026-02-24, `position-anchor` 2026-09-14 | below the floor: a docked position inside `@supports not (anchor-name: --a)` | DOM-09 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/CSS/Guides/Anchor_positioning/Using ; https://drafts.csswg.org/css-anchor-position-1/ |
| `hidden-until-found` | `hidden="until-found"` and the `beforematch` event | 102 (BCD `html.global_attributes.hidden.until-found`, `api.Element.beforematch_event`) | limited (not Baseline) | none on Chromium | DOM-09 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Global_attributes/hidden |
| `details-name` | `name` on `<details>` for exclusive accordions | 120 (BCD `html.elements.details.name`) | newly available, low 2024-09-03 | none | DOM-09 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/details |
| `move-before` | `Element.moveBefore()` and the custom-element `connectedMoveCallback()` | 133 (BCD `api.Element.moveBefore`) | limited (not Baseline) | below the floor: `insertBefore()`, which resets animations, focus and iframes | DOM-10 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/API/Element/moveBefore |
| `highlight` | CSS Custom Highlight API: `Highlight`, `CSS.highlights`, `::highlight()`; the `StaticRange()` constructor | 105 (BCD `api.Highlight`, `css.selectors.highlight`); `StaticRange()` 90 | newly available, low 2026-03-24 | none on Chromium | DOM-11 | 2026-09-23 | https://developer.mozilla.org/en-US/docs/Web/API/CSS_Custom_Highlight_API |
